peloton {
  persistence {
    store {
      type              = postgresql
      
      url               = "jdbc:postgresql://localhost:5432/peloton"
      user              = "peloton"
      password          = "peloton"
      maximum-pool-size = 10
    }
  }
}